const Button =({name})=>{
    return(
        <>
            <button className="px-3 py-1 m-3 bg-gray-100 rounded-lg hover:bg-black hover:text-white">{name}</button>
        </>
    )
} 

export default Button